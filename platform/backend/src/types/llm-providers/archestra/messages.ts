/**
 * Archestra message schemas - OpenAI-compatible
 *
 * The Archestra proxy speaks an OpenAI-compatible API, so we re-export OpenAI
 * schemas.
 */
export { MessageParamSchema, ToolCallSchema } from "../openai/messages";
