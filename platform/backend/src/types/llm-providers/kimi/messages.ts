/**
 * Kimi (Moonshot AI) message schemas - OpenAI-compatible
 *
 * Kimi uses an OpenAI-compatible API, so we re-export OpenAI schemas.
 * @see https://platform.moonshot.ai/docs/api/chat
 */
export { MessageParamSchema, ToolCallSchema } from "../openai/messages";
